services:
  - type: web
    name: jee-x2-planner
    env: node
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: db-storage
      mountPath: /opt/render/project/src/data
      sizeGB: 1
